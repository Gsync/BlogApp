<% include ./partials/header %>

<h1>index page</h1>

<% blogs.forEach(function(blog) { %>
  <div>
    <li><%=blog.title%></li>
    <img src="<%=blog.image%>">
    <span><%=blog.created%></span>
    <p><%=blog.body%></p>
  </div>
<%});%>

<% include ./partials/footer %>